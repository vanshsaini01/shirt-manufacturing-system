spring:
  application:
    name: delivery-dispatch-service
  config:
    import: "configserver:http://localhost:8888"
  datasource:
    url: jdbc:h2:mem:dispatchdb
    driver-class-name: org.h2.Driver
    username: vanshsaini1
    password: vansh@
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
